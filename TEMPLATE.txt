# This is the info file for a painting image. To use it, name it exactly
# like the image but with .txt instead — e.g. "sunset.jpg" -> "sunset.txt" —
# and put it in this same pictures/ folder.
#
# Rules:
#   - Lines starting with # are comments and are ignored.
#   - Blank lines are ignored.
#   - "title:" and "meta:" are one line each.
#   - "description:" must come LAST. Everything after it (this line and
#     all following lines, however many) becomes the description, so feel
#     free to write it across several lines.
#   - If a painting has no .txt file at all, it will just be labelled
#     "Unnamed" with no metadata or description — that's fine too.
#
# After adding or editing .txt files, run:
#     python3 generate_manifest.py

title: Morning Field
meta: 2023 · Oil on canvas · 80 × 60 cm
description:
  Painted over three mornings in early spring. The haze that sits low
  over the fields before the sun breaks through was the starting point —
  that particular grey-gold light that makes everything feel suspended.
